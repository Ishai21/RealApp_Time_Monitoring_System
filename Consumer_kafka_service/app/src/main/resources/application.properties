# Spring's web server port (the producer uses 8080, so the consumer takes 8081).
# Note: Prometheus metrics are NOT served here - they live on port 8082,
# started by PrometheusConfig.java.
server.port=8081
